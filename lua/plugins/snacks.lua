return {
  "folke/snacks.nvim",
  opts = {
    explorer = {
      -- Set to true to show gitignored files (like the target folder) by default
      ignored = true,
      -- Optional: show hidden files (.env, etc.) by default
      hidden = false,
    },
  },
}
